(*
   Tests for Chapter4.
   *)

(*open Fpinocaml_chapter4.A;;*)

let check msg actual expected = Alcotest.(check (bool)) msg true (actual = expected)






(*let tests = [("list empty", `Quick, test_empty);]*)
let tests = []

